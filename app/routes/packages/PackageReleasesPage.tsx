import { PackageHeader } from "./components/PackageHeader";

const PackageReleasesPage = () => {
  return (
    <div>
      <PackageHeader currentPath="releases" />

      <div>Releases</div>
    </div>
  );
};

export default PackageReleasesPage;
